# Optional NapCat runtime settings. Installed as /etc/napcat.env with mode 0600.
# Leave both values commented unless the server really needs a stable SOCKS5 proxy.
# Do not rotate proxy exits frequently; changing the login network can increase risk.
# NAPCAT_PROXY_ADDRESS=127.0.0.1
# NAPCAT_PROXY_PORT=1080
